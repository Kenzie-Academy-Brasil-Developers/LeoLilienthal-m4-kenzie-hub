import styled from "styled-components";

export const StyledDiv = styled.div`
  background: var(--Grey-4);
  height: 100vh;

  div {
    padding-left: 18.5vw;
  }

  div > h3 {
    font-weight: 700;
    font-size: 1.125rem;
    color: var(--Grey-0);
    padding-bottom: 1rem;
  }
  div > h4 {
    font-weight: 400;
    color: var(--Grey-0);
  }
`;
